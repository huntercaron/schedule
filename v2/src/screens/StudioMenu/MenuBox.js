import styled               from 'styled-components';

export default styled.section`
    font-weight: 500;
    display: flex;
    flex-wrap: wrap;

    p {
        max-width: 590px;
        margin: auto;
    }
`;
