import styled               from 'styled-components';
//import { colors }           from '../../constants';

export default styled.section`
    font-weight: 500;

    max-width: 600px;
    margin: 40px auto;
`;
