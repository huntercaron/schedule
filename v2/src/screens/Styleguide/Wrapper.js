import styled               from 'styled-components';
//import { colors }           from '../../constants';

export default styled.section`
    font-weight: 500;
    display: flex;

    p {
        max-width: 590px;
        margin: auto;
    }
`;
